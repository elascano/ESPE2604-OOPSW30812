class BasicOperation {
    static add(addent1, addent2) {
        let sum = addent1 + addent2
        return sum;
    }

    static subtract(minuend, subtrahend) {
        let differrence = minuend - subtrahend
        return differrence;
    }
}

module.exports = BasicOperation;