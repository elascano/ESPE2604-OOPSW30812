# -*- coding: utf-8 -*-
"""hw11UnitTest.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1SvmlnJhhUFQva-cKGjRcmg2kUK898-JY
"""

# Commented out IPython magic to ensure Python compatibility.
# %%writefile /content/src/subtraction.py
# class Subtraction:
#     def calculate(self, minuend: float, subtrahend: float) -> float:
#         self.result: float
#         result: float = round(minuend - subtrahend, 2)
#         return result

import os
import sys
import unittest

os.makedirs('/content/src', exist_ok=True)

with open('/content/src/subtraction.py', 'w') as f:
    f.write('''class Subtraction:
    def calculate(self, minuend: float, subtrahend: float) -> float:
        self.result: float
        result: float = round(minuend - subtrahend, 2)
        return result
''')

sys.path.insert(0, '/content/src')

from subtraction import Subtraction


class TestSubtraction(unittest.TestCase):

    def test_subtraction_1(self):
        operation = Subtraction()
        minuend = 50.0
        subtrahend = 25.5
        result = operation.calculate(minuend, subtrahend)
        expected = 24.5
        is_correct = result == expected
        print(f"Test 1: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_2(self):
        operation = Subtraction()
        minuend = 100.0
        subtrahend = 45.75
        result = operation.calculate(minuend, subtrahend)
        expected = 54.25
        is_correct = result == expected
        print(f"Test 2: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_3(self):
        operation = Subtraction()
        minuend = 75.5
        subtrahend = 30.5
        result = operation.calculate(minuend, subtrahend)
        expected = 45.0
        is_correct = result == expected
        print(f"Test 3: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_4(self):
        operation = Subtraction()
        minuend = 88.8
        subtrahend = 43.2
        result = operation.calculate(minuend, subtrahend)
        expected = 45.6
        is_correct = result == expected
        print(f"Test 4: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertAlmostEqual(result, expected, places=2)

    def test_subtraction_5(self):
        operation = Subtraction()
        minuend = 120.5
        subtrahend = 70.5
        result = operation.calculate(minuend, subtrahend)
        expected = 50.0
        is_correct = result == expected
        print(f"Test 5: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_6(self):
        operation = Subtraction()
        minuend = 200.0
        subtrahend = 99.99
        result = operation.calculate(minuend, subtrahend)
        expected = 100.01
        is_correct = result == expected
        print(f"Test 6: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_7(self):
        operation = Subtraction()
        minuend = 45.25
        subtrahend = 20.25
        result = operation.calculate(minuend, subtrahend)
        expected = 25.0
        is_correct = result == expected
        print(f"Test 7: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_8(self):
        operation = Subtraction()
        minuend = 33.33
        subtrahend = 11.11
        result = operation.calculate(minuend, subtrahend)
        expected = 22.22
        is_correct = result == expected
        print(f"Test 8: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_subtraction_9(self):
        operation = Subtraction()
        minuend = 99.9
        subtrahend = 49.9
        result = operation.calculate(minuend, subtrahend)
        expected = 50.0
        is_correct = result == expected
        print(f"Test 9: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertAlmostEqual(result, expected, places=2)

    def test_subtraction_10(self):
        operation = Subtraction()
        minuend = 150.75
        subtrahend = 100.25
        result = operation.calculate(minuend, subtrahend)
        expected = 50.5
        is_correct = result == expected
        print(f"Test 10: {minuend} - {subtrahend} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)


if __name__ == "__main__":
    print("SUBTRACTION TESTS")
    unittest.main(argv=[''], verbosity=0, exit=False)

import os

# Create the directory if it doesn't exist
os.makedirs('/content/src', exist_ok=True)

# Commented out IPython magic to ensure Python compatibility.
# %%writefile /content/src/addition.py
# 
# class Addition:
#     def calculate(self, addend1: float, addend2: float) -> float:
#         self.result: float
#         result: float = addend1 + addend2
#         return result
#

import os
import sys
import unittest


os.makedirs('/content/src', exist_ok=True)

with open('/content/src/addition.py', 'w') as f:
    f.write('''class Addition:
    def calculate(self, addend1: float, addend2: float) -> float:
        self.result: float
        result: float = addend1 + addend2
        return result
''')

sys.path.insert(0, '/content/src')
from addition import Addition

class TestAddition(unittest.TestCase):

    def test_addition_1(self):
        operation = Addition()
        addend1, addend2 = 25.5, 14.5
        result = operation.calculate(addend1, addend2)
        expected = 40.0
        is_correct = result == expected
        print(f"Test 1: {addend1} + {addend2} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_2(self):
        operation = Addition()
        a, b = 18.3, 22.7
        result = operation.calculate(a, b)
        expected = 41.0
        is_correct = result == expected
        print(f"Test 2: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_3(self):
        operation = Addition()
        a, b = 7.25, 12.75
        result = operation.calculate(a, b)
        expected = 20.0
        is_correct = result == expected
        print(f"Test 3: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_4(self):
        operation = Addition()
        a, b = 33.6, 16.4
        result = operation.calculate(a, b)
        expected = 50.0
        is_correct = result == expected
        print(f"Test 4: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_5(self):
        operation = Addition()
        a, b = 42.8, 37.2
        result = operation.calculate(a, b)
        expected = 80.0
        is_correct = result == expected
        print(f"Test 5: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_6(self):
        operation = Addition()
        a, b = 10.5, 20.5
        result = operation.calculate(a, b)
        expected = 31.0
        is_correct = result == expected
        print(f"Test 6: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_7(self):
        operation = Addition()
        a, b = 15.25, 24.75
        result = operation.calculate(a, b)
        expected = 40.0
        is_correct = result == expected
        print(f"Test 7: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_8(self):
        operation = Addition()
        a, b = 5.55, 4.45
        result = operation.calculate(a, b)
        expected = 10.0
        is_correct = result == expected
        print(f"Test 8: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_9(self):
        operation = Addition()
        a, b = 60.75, 39.25
        result = operation.calculate(a, b)
        expected = 100.0
        is_correct = result == expected
        print(f"Test 9: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

    def test_addition_10(self):
        operation = Addition()
        a, b = 0.99, 0.01
        result = operation.calculate(a, b)
        expected = 1.0
        is_correct = result == expected
        print(f"Test 10: {a} + {b} = {result:.2f} ({'Correct' if is_correct else 'Incorrect'})")
        self.assertEqual(result, expected)

if __name__ == "__main__":
    print("➕ ADDITION TESTS ")
    unittest.main(argv=[''], verbosity=0, exit=False)

# Commented out IPython magic to ensure Python compatibility.
# %%writefile /content/src/division.py
# class Division:
#     def calculate(self, dividend: float, divisor: float) -> float:
#         self.quotient: float
#         if divisor == 0:
#             raise ValueError("Cannot divide by zero")
#         quotient: float = dividend / divisor
#         return quotient

import os
import sys
import unittest


os.makedirs('/content/src', exist_ok=True)

with open('/content/src/division.py', 'w') as f:
    f.write('''class Division:
    def calculate(self, dividend: float, divisor: float) -> float:
        self.quotient: float
        if divisor == 0:
            raise ValueError("Cannot divide by zero")
        quotient: float = dividend / divisor
        return quotient
''')


sys.path.insert(0, '/content/src')


from division import Division

class TestDivision(unittest.TestCase):

    def test_divide_exact_positives(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(20, 4)
        self.assertEqual(result, 5)

    def test_divide_positive_by_negative(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(15, -3)
        self.assertEqual(result, -5)

    def test_divide_negative_by_positive(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(-20, 5)
        self.assertEqual(result, -4)

    def test_divide_two_negatives(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(-30, -6)
        self.assertEqual(result, 5)

    def test_divide_by_one(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(100, 1)
        self.assertEqual(result, 100)

    def test_divide_with_decimals(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(7, 2)
        self.assertEqual(result, 3.5)

    def test_divide_number_by_itself(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(25, 25)
        self.assertEqual(result, 1)

    def test_divide_zero_by_number(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(0, 10)
        self.assertEqual(result, 0)

    def test_divide_with_floats(self) -> None:
        operation: Division = Division()
        result: float = operation.calculate(5.5, 2)
        self.assertEqual(result, 2.75)

    def test_divide_by_zero_raises_error(self) -> None:
        operation: Division = Division()
        with self.assertRaises(ValueError):
            operation.calculate(10, 0)


print("\nRUNNING DIVISION TESTS\n")
unittest.main(argv=[''], verbosity=2, exit=False)

!pip install ipytest

class Multiplication:
    def calculate(self, factor1: float, factor2: float) -> float:
        self.product: float
        product: float = factor1 * factor2
        return product

import os
import sys
import unittest


os.makedirs('/content/src', exist_ok=True)

with open('/content/src/multiplication.py', 'w') as f:
    f.write('''class Multiplication:
    def calculate(self, multiplicand: float, multiplier: float) -> float:
        self.product: float
        product: float = multiplicand * multiplier
        return product
''')


sys.path.insert(0, '/content/src')

from multiplication import Multiplication


class TestMultiplication(unittest.TestCase):

    def show_result(self, test_number, multiplicand, multiplier, result, expected):
        status = "Correct" if result == expected else "Incorrect"
        print(
            f"Test {test_number}: "
            f"{multiplicand} × {multiplier} = {result:.2f} "
            f"({status})"
        )

    def test_multiply_exact_positives(self) -> None:
        operation = Multiplication()
        multiplicand = 5
        multiplier = 4
        result = operation.calculate(multiplicand, multiplier)
        expected = 20
        self.show_result(1, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_positive_by_negative(self) -> None:
        operation = Multiplication()
        multiplicand = 6
        multiplier = -3
        result = operation.calculate(multiplicand, multiplier)
        expected = -18
        self.show_result(2, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_negative_by_positive(self) -> None:
        operation = Multiplication()
        multiplicand = -7
        multiplier = 2
        result = operation.calculate(multiplicand, multiplier)
        expected = -14
        self.show_result(3, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_two_negatives(self) -> None:
        operation = Multiplication()
        multiplicand = -8
        multiplier = -5
        result = operation.calculate(multiplicand, multiplier)
        expected = 40
        self.show_result(4, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_by_one(self) -> None:
        operation = Multiplication()
        multiplicand = 100
        multiplier = 1
        result = operation.calculate(multiplicand, multiplier)
        expected = 100
        self.show_result(5, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_by_zero(self) -> None:
        operation = Multiplication()
        multiplicand = 25
        multiplier = 0
        result = operation.calculate(multiplicand, multiplier)
        expected = 0
        self.show_result(6, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_with_decimals(self) -> None:
        operation = Multiplication()
        multiplicand = 2.5
        multiplier = 4
        result = operation.calculate(multiplicand, multiplier)
        expected = 10.0
        self.show_result(7, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_number_by_itself(self) -> None:
        operation = Multiplication()
        multiplicand = 9
        multiplier = 9
        result = operation.calculate(multiplicand, multiplier)
        expected = 81
        self.show_result(8, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_with_floats(self) -> None:
        operation = Multiplication()
        multiplicand = 3.5
        multiplier = 2
        result = operation.calculate(multiplicand, multiplier)
        expected = 7.0
        self.show_result(9, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)

    def test_multiply_zero_by_number(self) -> None:
        operation = Multiplication()
        multiplicand = 0
        multiplier = 15
        result = operation.calculate(multiplicand, multiplier)
        expected = 0
        self.show_result(10, multiplicand, multiplier, result, expected)
        self.assertEqual(result, expected)


print("\nRUNNING MULTIPLICATION TESTS\n")

unittest.main(argv=[''], verbosity=0, exit=False)